Procedure:
    type: object
    properties:
      # 以下工具路径允许为 string 或 null (对应 YAML 中的空值)
      msisensor-pro: {type: ["string", "null"]}

outdir:
    type: string
    default: "output"

reference: # path of your reference genome fasta file
  type: string
genome_version:
  type: string
sample_path: #path of the sample path, see the  template at demo/data_input/data4test/tumor_normal_paired.samples.info.tsv
  type: string
